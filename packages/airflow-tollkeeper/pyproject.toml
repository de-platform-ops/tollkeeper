[project]
name = "airflow-tollkeeper"
version = "0.1.0"
description = "Airflow operator wrapper for tollkeeper"
readme = "README.md"
authors = [
    { name = "Sadha Chilukoori", email = "srchilukoori@gmail.com" }
]
license = {text = "Apache-2.0"}
requires-python = ">=3.11"
dependencies = [
    "tollkeeper>=0.1.0",
    "apache-airflow>=2.9",
]

[project.optional-dependencies]
spark = ["apache-airflow-providers-apache-spark>=4.0"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=6.0",
    "ruff>=0.11",
    "sqlglot>=26.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/airflow_tollkeeper"]

[tool.pytest.ini_options]
addopts = "--cov=airflow_tollkeeper --cov-report=term-missing"

[tool.coverage.run]
source = ["airflow_tollkeeper"]

[tool.coverage.report]
fail_under = 80
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]

[tool.uv.sources]
tollkeeper = { workspace = true }

[tool.ruff]
line-length = 120
target-version = "py311"
