## What problem does this solve?

<!-- Describe the bug or missing feature. Link the issue if one exists. -->

## How was this tested?

<!-- Commands run, cases covered. "Tests pass" alone is not enough. -->

## Checklist

- [ ] I searched for existing PRs (open and closed) that address this
- [ ] A human reviewed the complete diff
- [ ] Tests pass locally (`uv run pytest`)
- [ ] Lint passes (`uv run ruff check .` and `uv run ruff format --check .`)
- [ ] AI tools were used (specify which): 
